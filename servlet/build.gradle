jar {
    baseName="fuava-ctprofiler-servlet"
    manifest {
        attributes 'Implementation-Title': 'Fuava Calltree-Profiler Servlet helpers',
                'Implementation-Version': version
    }
}

dependencies {
    compile 'javax.servlet:javax.servlet-api:3.1.0'
    compile project(":core")    
}

